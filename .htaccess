RewriteEngine On
RewriteBase /

# Nếu là file/thư mục thật thì chạy trực tiếp
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Còn lại đẩy vào router
RewriteRule ^(.+)$ index.php?route=$1 [QSA,L]
RewriteRule ^$ index.php [L]
